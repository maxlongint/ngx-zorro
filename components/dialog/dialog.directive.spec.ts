import { NgxDialogDirective } from './dialog.directive';

describe('DialogDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxDialogDirective();
        expect(directive).toBeTruthy();
    });
});
