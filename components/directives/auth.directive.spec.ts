import { NgxAuthDirective } from './auth.directive';

describe('AuthDirective', () => {
    it('should create an instance', () => {
        // const directive = new NgxAuthDirective();
        // expect(directive).toBeTruthy();
    });
});
